version: 1

# gh's default is https, and this repo signs and pushes over ssh.
git_protocol: ssh

# Not a gh default any more: it shipped `co` until they dropped the built-in
# aliases, and `gh alias list` on a bare config now comes back empty.
aliases:
  co: pr checkout
